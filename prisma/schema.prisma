// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_POSTGRES_PRISMA_URL")
  
  // Caso o seu arquivo tenha essa linha abaixo, altere também:
  directUrl = env("DATABASE_POSTGRES_URL_NON_POOLING") 
}

model Categoria {
  id       Int       @id @default(autoincrement())
  nome     String    @unique @db.VarChar(50)
  projetos Projeto[]

  @@map("categorias")
}

model Projeto {
  id              Int             @id @default(autoincrement())
  titulo          String          @db.VarChar(100)
  descricao       String          @db.Text
  categoriaId     Int
  categoria       Categoria       @relation(fields: [categoriaId], references: [id])
  imagemPrincipal String
  galeria         String[]        @default([]) // <--- ADICIONE ESTA LINHA
  dataPublicacao  DateTime        @default(now())
  ativo           Boolean         @default(true)

  // Ficha Técnica
  localizacao     String          @default("A definir") @db.VarChar(200)
  area            String          @default("A definir") @db.VarChar(50)

  imagens         ImagemProjeto[]

  @@map("projetos")
}

model ImagemProjeto {
  id        Int     @id @default(autoincrement())
  projetoId Int
  projeto   Projeto @relation(fields: [projetoId], references: [id], onDelete: Cascade)
  imageUrl  String

  @@map("imagens_projeto")
}
